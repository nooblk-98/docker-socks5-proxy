# Optional: set credentials for authenticated SOCKS5
# Leave blank for an open (unauthenticated) proxy
PROXY_USER=
PROXY_PASS=

# Host port to expose the SOCKS5 server on (default: 1080)
SOCKS5_PORT=54178
